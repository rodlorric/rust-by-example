fn main(){
    // This is an example of a line comment
    // println!("Hello, world!");
    

    /*
     * This is another type of comment, the block comment.
     * Line comments are recommended over the block comments.
     */

    /*
    Note, there is no actual need to add the `*` column
    */

    let x = 5 + /* 90 + */ 5;
    println!("Is `x` 10 or 100? x = {}", x);
}
